node_modules/
*.log
.DS_Store
Thumbs.db

# Built compendium packs (LevelDB) — compiled from src/packs via `npm run pack`.
# Root-anchored so it ignores ONLY the top-level built packs/, never src/packs/.
/packs/

# Local working / planning docs — not part of the published module.
/RELEASE-STAGING.md
/VENDORING-REPORT.md

# Verbatim source-book text extracts used only as audit working files — NEVER commit (copyright).
import-staging/**/*_fulltext.txt
import-staging/**/*-prose-dump.txt
import-staging/**/booktext/
